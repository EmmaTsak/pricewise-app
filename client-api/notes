Clean up the socket setup:
move the backend/frontend URLs into environment variables instead of hardcoding localhost values.
Harden the email flow a bit:
validate email format, improve error messages, and confirm the Nodemailer config is safe and reliable.
Review the scraper schedule and startup behavior:
decide whether immediate scrape-on-start should stay in production and confirm the cron timing you want.
Do a final UI cleanup pass:
check spacing, button consistency, image fallbacks, and mobile behavior one more time.
Remove dead code and leftovers from earlier versions:
unused imports, commented old scraper calls, old alert-based logic, duplicate styling.
Verify translations completely:
make sure there are no missing EN/GR keys in navbar, footer, home, about, privacy, shopping list, compare modal, and toast messages.
Prepare production environment setup:
backend .env, frontend API/socket URL config, database URL, email credentials, and allowed CORS origins.
Do deployment prep:
build frontend and backend, run Prisma migration in the target environment, and test the deployed app.
After deployment prep, do one final MVP checklist:
product browsing, filters, compare, shopping list, email sending, live refresh, privacy page, translations, and mobile navigation.